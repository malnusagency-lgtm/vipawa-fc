import GalleryContent from '@/components/GalleryContent';

export const metadata = {
    title: 'Gallery | Vipawa Ladies FC',
    description: 'Browse photos from Vipawa Ladies FC — match days, training sessions, community events, and more.',
};

export default function GalleryPage() {
    return <GalleryContent />;
}
