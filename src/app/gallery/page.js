import GalleryContent from '@/components/GalleryContent';

export const metadata = {
    title: 'Gallery | Vipawa Ladies CF',
    description: 'Browse photos from Vipawa Ladies CF — match days, training sessions, community events, and more.',
};

export default function GalleryPage() {
    return <GalleryContent />;
}

